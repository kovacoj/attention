[project]
name = "hvle"
version = "0.1.0"
description = "Experiments for precision placement in attention"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch>=2.12.0",
    "transformers>=4.52.0",
    "matplotlib>=3.7.0",
    "numpy>=1.26.0",
]
