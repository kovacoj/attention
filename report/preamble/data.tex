%% Thesis type: bachelor, master, doctoral
%% Reused by TeXtured metadata even though this document is a report.
\newcommand*{\ThesisType}{report}

%% Document title and metadata
\newcommand*{\ThesisTitle}{Mixed-Precision Randomized Sketching for Scaled Dot-Product Attention}
\newcommand*{\ThesisTitlePlaintext}{Mixed-Precision Randomized Sketching for Scaled Dot-Product Attention}
\newcommand*{\ThesisTitleFront}{\ThesisTitle}

%% Author
\newcommand*{\ThesisAuthor}{J\'an Kova\v{c}ovsk\'y}
\newcommand*{\ThesisAuthorPlaintext}{Ján Kovačovský}

%% Submission year
\newcommand*{\YearSubmitted}{2026}

%% Basic affiliation/course metadata
\newcommand*{\University}{Charles University}
\newcommand*{\Department}{Faculty of Mathematics and Physics}
\newcommand*{\DeptType}{Faculty}
\newcommand*{\Supervisor}{H. V. Le}
\newcommand*{\SupervisorsDepartment}{Course instructor}
\newcommand*{\StudyProgramme}{NMAG469 -- Mathematical Foundations of Machine Learning}
\newcommand*{\ReportType}{Term Paper Draft}
\newcommand*{\ReportCourse}{Mathematical Foundations of Machine Learning}
\newcommand*{\ReportAffiliation}{Faculty of Mathematics and Physics, Charles University}

%% Abstract
\newcommand*{\Abstract}{%
    We propose a mathematically focused study of scaled dot-product attention under two simultaneous approximations: randomized feature sketching and mixed-precision arithmetic. The exact logit operator $d^{-1/2}QK^T$ is replaced by the sketched operator $d^{-1/2}QSS^TK^T$, where $S$ is a random sketching matrix of smaller column dimension. The report isolates three levels of perturbation: the deterministic error in the logits, the induced perturbation of the rowwise softmax map, and the additional finite-precision error introduced when the sketched computation is carried out in low precision. The main analytical result is a clean error decomposition showing that the total output error is controlled by the sum of a sketching term and a rounding term. The accompanying experiments are designed as a validation of this decomposition rather than as a large-scale engineering benchmark.
}

%% Subject (short description for PDF metadata)
\newcommand*{\Subject}{%
    Mixed precision and randomized sketching for transformer attention.
}

%% Keywords (about 3-7)
\newcommand*{\Keywords}{%
    mixed-precision arithmetic, randomized numerical linear algebra, attention, transformers, sketching
}
\newcommand*{\KeywordsPlaintext}{%
    mixed-precision arithmetic, randomized numerical linear algebra, attention, transformers, sketching
}
